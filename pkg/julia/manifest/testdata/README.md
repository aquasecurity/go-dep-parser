The manifest files in this directory can be generated by resolving this project.
Keep the project file and the `src/` directory present so this keeps working for future Julia versions.

```
julia +1.8
]resolve
```
